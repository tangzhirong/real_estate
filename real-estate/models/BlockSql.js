// CRUD SQL语句
var block = {
	queryByDistrict: 'select * from community where district=?',
	queryAllBlocks: 'select * from community LIMIT ?,5',
	queryByTitle:'select * from community where title = ?',
	queryByAddress:'SELECT * FROM community where address=?',


	queryByPlotRate1:'SELECT COUNT(1) AS num FROM community WHERE plot_rate<1.0 and district=?',
	queryByPlotRate2:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=1.0 and plot_rate<1.5 and district=?',
	queryByPlotRate3:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=1.5 and plot_rate<2.0 and district=?',
	queryByPlotRate4:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=2.0 and plot_rate<2.5 and district=?',
	queryByPlotRate5:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=2.5 and plot_rate<3.0 and district=?',
	queryByPlotRate6:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=3.0 and plot_rate<3.5 and district=?',
	queryByPlotRate7:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=3.5 and plot_rate<4.0 and district=?',
	queryByPlotRate8:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=4.0 and plot_rate<4.5 and district=?',
	queryByPlotRate9:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=4.5 and plot_rate<5.0 and district=?',
	queryByPlotRate10:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=5.0 and district=?',
	queryByPrice:'SELECT unit_price FROM  community WHERE district=?',
	queryByTime1:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>2011 and district=?',
	queryByTime2:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>=2006 and YEAR(build_time)<=2011 and district=?',
	queryByTime3:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>=2001 and YEAR(build_time)<=2006 and district=?',
	queryByTime4:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>=1996 and YEAR(build_time)<=2001 and district=?',
	queryByTime5:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>=1991 and YEAR(build_time)<=1996 and district=?',
	queryByTime6:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>=1986 and YEAR(build_time)<=1991 and district=?',
	queryByTime7:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)<=1986 and district=?',



	/*我写的*/
	queryByBuildAge:'select * from community where district=? order by build_time asc',
    queryByBuildAgeDesc:'select * from community where district=? order by build_time desc',
	queryByBuildPrice:'select * from community where district=? order by unit_price asc',

	queryByBuildPriceDesc:'select * from community where district=? order by unit_price desc',




	queryByPlotRate1:'SELECT COUNT(1) AS num FROM community WHERE plot_rate<1.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate2:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=1.0 and plot_rate<1.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate3:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=1.5 and plot_rate<2.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate4:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=2.0 and plot_rate<2.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate5:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=2.5 and plot_rate<3.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate6:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=3.0 and plot_rate<3.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate7:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=3.5 and plot_rate<4.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate8:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=4.0 and plot_rate<4.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate9:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=4.5 and plot_rate<5.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPlotRate10:'SELECT COUNT(1) AS num FROM community WHERE plot_rate>=5.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryByPrice:'SELECT unit_price FROM community WHERE district=? and YEAR(build_time) between ? and ? and plot_rate between ? and ?',
	queryByTime1:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>2011 and district=? and unit_price between ? and ?',
	queryByTime2:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>2006 and YEAR(build_time)<=2011 and district=? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByTime3:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>2001 and YEAR(build_time)<=2006 and district=? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByTime4:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>1996 and YEAR(build_time)<=2001 and district=? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByTime5:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>1991 and YEAR(build_time)<=1996 and district=? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByTime6:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)>1986 and YEAR(build_time)<=1991 and district=? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByTime7:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time)<=1986 and district=? and unit_price between ? and ? and plot_rate between ? and ?',


	queryByPlotRate1:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate<1.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate2:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=1.0 and plot_rate<1.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate3:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=1.5 and plot_rate<2.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate4:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=2.0 and plot_rate<2.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate5:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=2.5 and plot_rate<3.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate6:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=3.0 and plot_rate<3.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate7:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=3.5 and plot_rate<4.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate8:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=4.0 and plot_rate<4.5 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate9:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=4.5 and plot_rate<5.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate10:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=5.0 and district=? and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRate1WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate<1.0 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate2WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=1.0 and community.plot_rate<1.5 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate3WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=1.5 and community.plot_rate<2.0 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate4WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=2.0 and community.plot_rate<2.5 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate5WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=2.5 and community.plot_rate<3.0 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate6WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=3.0 and community.plot_rate<3.5 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate7WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=3.5 and community.plot_rate<4.0 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate8WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=4.0 and community.plot_rate<4.5 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate9WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=4.5 and community.plot_rate<5.0 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPlotRate10WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.plot_rate>=5.0 and community.district=? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.build_type LIKE "%"?"%"',
	queryByPrice:'SELECT unit_price FROM  community WHERE district=? and YEAR(build_time) between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByPriceWithBusiness:'SELECT community.unit_price from (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id where community.district = ? and commercial_area.name=? and YEAR(community.build_time) between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime1:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>2011 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime2:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>2006 and YEAR(build_time)<=2011 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime3:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>2001 and YEAR(build_time)<=2006 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime4:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>1996 and YEAR(build_time)<=2001 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime5:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>1991 and YEAR(build_time)<=1996 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime6:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>1986 and YEAR(build_time)<=1991 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime7:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)<=1986 and district=? and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTime1WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)>2011 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime2WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)>2006 and YEAR(community.build_time)<=2011 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime3WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)>2001 and YEAR(community.build_time)<=2006 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime4WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)>1996 and YEAR(community.build_time)<=2001 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime5WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)>1991 and YEAR(community.build_time)<=1996 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime6WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)>1986 and YEAR(community.build_time)<=1991 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',
	queryByTime7WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE YEAR(community.build_time)<=1986 and community.district = ? and commercial_area.name = ? and community.unit_price between ? and ? and community.plot_rate between ? and ? and community.build_type LIKE "%"?"%"',

	queryBlocksByPlotRate:'SELECT * FROM community WHERE district=? and YEAR(build_time) between ? and ? and unit_price between ? and ?',
	queryBlocksByPrice:'SELECT * from community where district=? and YEAR(build_time) between ? and ? and plot_rate between ? and ?',
	queryBlocksByTime:'SELECT * FROM community WHERE district=? and unit_price between ? and ? and plot_rate between ? and ?',
  //不同维度总计
  //价钱维度
	queryByPriceAll:'SELECT unit_price FROM community WHERE YEAR(build_time) between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	//房龄维度
	//queryByTimeAll:'SELECT COUNT(1) AS num FROM community WHERE YEAR(build_time) is not null and unit_price between ? and ? and plot_rate between ? and ?',
	queryByTimeAll1:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>2011 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTimeAll2:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>2006 and YEAR(build_time)<=2011 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTimeAll3:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>2001 and YEAR(build_time)<=2006 and 2006 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTimeAll4:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>1996 and YEAR(build_time)<=2001 and 2001 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTimeAll5:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>1991 and YEAR(build_time)<=1996 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	queryByTimeAll6:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time)>1986 and YEAR(build_time)<=1991 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
  queryByTimeAll7:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE YEAR(build_time) <=1986 and unit_price between ? and ? and plot_rate between ? and ? and build_type LIKE "%"?"%"',
	//容积率维度
	queryByPlotRateAll1:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate<1.0 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll2:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=1.0 and plot_rate<1.5 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll3:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=1.5 and plot_rate<2.0 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll4:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=2.0 and plot_rate<2.5 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll5:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=2.5 and plot_rate<3.0 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll6:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=3.0 and plot_rate<3.5 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll7:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=3.5 and plot_rate<4.0 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll8:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=4.0 and plot_rate<4.5 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll9:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=4.5 and plot_rate<5.0 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
	queryByPlotRateAll10:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE plot_rate>=5.0 and YEAR(build_time) between ? and ? and unit_price between ? and ? and build_type LIKE "%"?"%"',
  	//建筑类型维度
	queryByBuildTypeAll1:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%板房%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildTypeAll2:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%小高层%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildTypeAll3:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%高层%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildTypeAll4:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%公寓%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildTypeAll5:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%联排别墅%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildTypeAll6:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%双排别墅%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildTypeAll7:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE build_type LIKE "%花园洋房%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',

	queryByBuildType1:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%板房%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType2:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%小高层%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType3:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%高层%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType4:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%公寓%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType5:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%联排别墅%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType6:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%双排别墅%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType7:'SELECT COUNT(1) AS num, SUM(unit_price)/COUNT(1) AS avg_price FROM community WHERE district = ? and build_type LIKE "%花园洋房%" and YEAR(build_time) between ? and ? and unit_price between ? and ? and plot_rate between ? and ?',
	queryByBuildType1WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%板房%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',
	queryByBuildType2WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%小高层%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',
	queryByBuildType3WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%高层%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',
	queryByBuildType4WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%公寓%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',
	queryByBuildType5WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%联排别墅%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',
	queryByBuildType6WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%双排别墅%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',
	queryByBuildType7WithBusiness:'SELECT COUNT(1) AS num, SUM(community.unit_price)/COUNT(1) AS avg_price FROM (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id WHERE community.district = ? and commercial_area.name = ? and community.build_type LIKE "%花园洋房%" and YEAR(community.build_time) between ? and ? and community.unit_price between ? and ? and community.plot_rate between ? and ?',

	queryBusinessByDistrict:'SELECT * FROM commercial_area where address like ?',
	queryBlocksByBusiness:"select community.* from (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id where commercial_area.name=?",
	queryBusinessByName:'select * from commercial_area where name=?',
	queryBlocksByBusiness_BuildAge:"select community.* from (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id where commercial_area.name=? order by build_time asc",
	queryBlocksByBusiness_BuildAgeDesc:"select community.* from (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id where commercial_area.name=? order by build_time desc",
	queryBlocksByBusiness_BuildPrice:"select community.* from (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id where commercial_area.name=? order by unit_price asc",
	queryBlocksByBusiness_BuildPriceDesc:"select community.* from (community left join community_commercial_relation on community.id=community_commercial_relation.community_id) left join commercial_area on commercial_area.id=community_commercial_relation.commercial_area_id where commercial_area.name=? order by unit_price desc",
};


module.exports = block;
